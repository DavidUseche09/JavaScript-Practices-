// Declarativas 
function suma(a,b) {
    var resultado = a + b;
    return resultado;
}
suma(5,5);

// Expresion 
var suma = function (a,b) {
    var resultado = a + b;
    return resultado;
}
suma(5,5)

// Ejemplos 
function age(age) {
    console.log(`Your current age is: ${age}`);
}

// Nota: Siempre usar las comillas correspondientes al adding la info. / Usar llaves {} al poner el signo de pesos $ //
// {console.log(`Tu nombre es: ${name}`);} //
